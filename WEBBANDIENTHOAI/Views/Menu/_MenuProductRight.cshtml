@model IEnumerable<WEBBANDIENTHOAI.Models.MFC.ProductCategory>

<aside class="special-home-right">
    <h6 class="bg-blue text-center text-white mb-0 p-2">Danh mục phổ biến</h6>
    @if (Model != null && Model.Any())
    {
        foreach (var item in Model)
        {


            if (ViewBag.CateId == item.Id)
            {
                <div class="card-banner border-bottom">
                    <div class="py-3" style="width:80%">
                        @*<div>
                                @Html.ActionLink(item.Name, "ProductCategory", "Category", new { @Id = item.Id }, null)
                            </div>*@


                        @*<a href="@Url.Action("ProductCategory", "Category", new { Id = item.Id })" class="btn btn-secondary btn-sm"> Xem ngay </a>*@


                        @*</a>*@
                        <div class="banner_category">
                            <a href="/danh-muc-san-pham/@item.Alias">@item.Title</a>
                        </div>
                        @*<div class="banner_item align-items-center" style="background-image: url(@item.Icon)">*@
                        <a href="#" class="btn btn-secondary btn-sm"> Xem ngay </a>


                        <img src="@item.Icon" height="80" class="img-bg">
                        @*<a href="@Url.Action("ProductCategory", "Category", new { Id = item.Id })">*@
                    </div>
                </div>
            }
            else
            {
                <div class="card-banner border-bottom">
                    <div class="py-3" style="width:80%">
                        @*<div>
                                @Html.ActionLink(item.Name, "ProductCategory", "Category", new { @Id = item.Id }, null)
                            </div>*@


                        @*<a href="@Url.Action("ProductCategory", "Category", new { Id = item.Id })" class="btn btn-secondary btn-sm"> Xem ngay </a>*@


                        @*</a>*@
                        <div class="banner_category">
                            <a href="/danh-muc-san-pham/@item.Alias-@(item.Id)">@item.Title</a>
                        </div>
                        @*<div class="banner_item align-items-center" style="background-image: url(@item.Icon)">*@
                        <a href="/danh-muc-san-pham/@item.Alias-@(item.Id)" class="btn btn-secondary btn-sm"> Xem ngay </a>


                        <a href="/danh-muc-san-pham/@item.Alias-@(item.Id)"> <img src="@item.Icon" height="80" class="img-bg"></a>
                        @*<img  src="@item.Icon" height="80" class="img-bg">*@
                        @*<a href="@Url.Action("ProductCategory", "Category", new { Id = item.Id })">*@
                    </div>
                </div>

            }





        }
    }

</aside>